esphome:
  name: lighting-485-16ch
  friendly_name: 485 16路照明模块
  on_boot:
    - priority: -100  # 确保在其他启动任务之前执行
      then:
      - delay: 2s # 开机等待2秒，确保 UART 硬件准备就绪
      - script.execute: poll_script     # 开机先刷新一次状态
esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf
wifi: # 配置 WiFi，建议修改为自己的网络
  #ssid: "your_wifi_ssid"
  #password: "your_wifi_password"
  ap: # 启用 AP 模式，方便初始配置或 WiFi 连接失败时连接
    ssid: "Lighting485-16ch"
    password: "12345678" 
ota: # 开启 OTA 升级
  - platform: esphome
    password: "12345678" # OTA 密码，建议修改为更安全的密码

api: # 开启 Home Assistant API
  encryption:
    key: "aTXEgWQIHndCXK3/e/IckmFXbf7csBLTlZ7mNJoP87M=" # 建议后续设置一个安全的加密密钥  

web_server: # 开启 Web 服务器，方便调试和手动控制 不需要的话可以删除这一段
  port: 80

logger:
  level: DEBUG # 输出调试日志，方便查看通信细节 后续调试完成后可以改为 INFO 或 WARNING 减少日志量     
captive_portal:  # 启用 Captive Portal 功能，方便在 WiFi 连接失败时自动弹出配置界面 不需要的话可以删除这一段  

uart:
  id: uart485
  tx_pin: GPIO20 #tx 接灯控板的 RX，GPIO17 只是示例，实际接线请根据需要修改
  rx_pin: GPIO21 #rx 接灯控板的 TX，GPIO16 只是示例，实际接线请根据需要修改
  baud_rate: 9600
  parity: NONE  # 无校验
  data_bits: 8
  stop_bits: 1

# ====================== 断电记忆======================
globals:
  - id: last_poll_seconds  # 全局变量，记录上次设置的轮询秒数
    type: int
    restore_value: true
    initial_value: '5'     # 开机默认5秒，可改成0

number:
  - platform: template  
    name: "轮询间隔秒-0=关闭"  
    id: poll_interval_num
    icon: mdi:timer
    optimistic: true # 直接根据命令更新状态，不等待实际硬件反馈
    min_value: 0 # 0表示关闭轮询
    max_value: 60
    step: 1
    mode: slider
    unit_of_measurement: "秒"
    initial_value: 5 # 开机默认5秒，可改成0
    on_value:
      - globals.set:
          id: last_poll_seconds
          value: !lambda 'return x;'
      - script.execute: poll_script   # 立即生效

# 手动刷新按钮
button:
  - platform: template
    name: "手动刷新状态"
    icon: mdi:refresh
    on_press:
      - script.execute: poll_script

  - platform: template
    name: "全部灯 开"
    icon: mdi:lightbulb-group
    on_press:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0xFF, 0xFF, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "全部灯关off"
    icon: mdi:lightbulb-group-off
    on_press:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0xFF, 0xFF, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

# ====================== 脚本 ======================
script:
  - id: read_status
    then:
      - delay: 80ms
      - lambda: |-
          if (id(uart485).available() >= 5) {
            uint8_t buf[5];
            id(uart485).read_array(buf, 5);
            uint8_t calc = (buf[0] + buf[1] + buf[2] + buf[3]) & 0xFF;
            if (buf[0] == 0xFA && calc == buf[4]) {
              uint8_t xh = buf[2], xl = buf[3];
              id(light_1).publish_state((xh & 0x80) != 0);
              id(light_2).publish_state((xh & 0x40) != 0);
              id(light_3).publish_state((xh & 0x20) != 0);
              id(light_4).publish_state((xh & 0x10) != 0);
              id(light_5).publish_state((xh & 0x08) != 0);
              id(light_6).publish_state((xh & 0x04) != 0);
              id(light_7).publish_state((xh & 0x02) != 0);
              id(light_8).publish_state((xh & 0x01) != 0);
              id(light_9).publish_state((xl & 0x80) != 0);
              id(light_10).publish_state((xl & 0x40) != 0);
              id(light_11).publish_state((xl & 0x20) != 0);
              id(light_12).publish_state((xl & 0x10) != 0);
              id(light_13).publish_state((xl & 0x08) != 0);
              id(light_14).publish_state((xl & 0x04) != 0);
              id(light_15).publish_state((xl & 0x02) != 0);
              id(light_16).publish_state((xl & 0x01) != 0);
            }
          }

  - id: poll_script
    then:
      - lambda: |-
          uint8_t q[5] = {0xF5, 0x10, 0x00, 0x00, 0x00};
          q[4] = (q[0] + q[1] + q[2] + q[3]) & 0xFF;
          id(uart485).write_array(q, 5);
      - delay: 120ms
      - script.execute: read_status

  # 轮询脚本（带 3 次重试）
  #- id: poll_script
    #then:
      #- repeat:
        #  count: 3
         # then:
          #  - lambda: |-
           #     id(rs485_de).turn_on();
           #     delay(5);
           #     uint8_t q[5] = {0xF5, 0x10, 0x00, 0x00, 0x00};
            #    q[4] = (q[0] + q[1] + q[2] + q[3]) & 0xFF;
            #    id(uart485).write_array(q, 5);
             #   delay(5);
             #   id(rs485_de).turn_off();
            #- delay: 120ms
           # - script.execute: read_status
# 定时轮询（根据 number 设置）
interval:
  - interval: 30s # 每秒检查一次是否需要轮询
    then:
      - if:
          condition:
            lambda: 'return id(last_poll_seconds) > 0;'
          then:
            - lambda: |-
                static uint8_t cnt = 0;
                if (++cnt >= id(last_poll_seconds)) {
                  id(poll_script).execute();
                  cnt = 0;
                }


# ====================== 16路开关======================
# 灯 2~灯 8、灯 10~灯 16 复制上面模板，只改 name / id / 掩码 即可
  # 掩码表（直接替换 0x80, 0x00 这两字节）：
  # 1: 80 00   2: 40 00   3: 20 00   4: 10 00
  # 5: 08 00   6: 04 00   7: 02 00   8: 01 00
  # 9: 00 80  10: 00 40  11: 00 20  12: 00 10
  # 13:00 08  14:00 04  15:00 02  16:00 01
switch:
  - platform: template  # 每个灯都用一个 template switch 来控制，on/off action 里发送对应的命令
    name: "灯 1" # 需要修改为 "灯 2" ~ "灯 16"
    id: light_1 # 需要修改为 light_2 ~ light_16，id 用于在脚本里更新状态
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x80, 0x00, 0x00}; //需要修改掩码字节为对应灯的掩码
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x80, 0x00, 0x00}; //需要修改掩码字节为对应灯的掩码
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 2"
    id: light_2
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x40, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x40, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 3"
    id: light_3
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x20, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x20, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 4"
    id: light_4
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x10, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x10, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 5"
    id: light_5
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x08, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x08, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 6"
    id: light_6
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x04, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x04, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 7"
    id: light_7
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x02, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x02, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 8"
    id: light_8
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x01, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x01, 0x00, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 9"
    id: light_9
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x80, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x80, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 10"
    id: light_10
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x40, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x40, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 11"
    id: light_11
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x20, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x20, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 12"
    id: light_12
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x10, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x10, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 13"
    id: light_13
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x08, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x08, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 14"
    id: light_14
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x04, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x04, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 15"
    id: light_15
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x02, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x02, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status

  - platform: template
    name: "灯 16"
    id: light_16
    optimistic: false
    turn_on_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x51, 0x00, 0x01, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status # 开灯后立即刷新状态，确保 Home Assistant 显示正确
    turn_off_action:
      - lambda: |-
          uint8_t pkt[5] = {0xF5, 0x52, 0x00, 0x01, 0x00};
          pkt[4] = (pkt[0]+pkt[1]+pkt[2]+pkt[3]) & 0xFF;
          id(uart485).write_array(pkt, 5);
      - delay: 80ms
      - script.execute: read_status
sensor:
  - platform: wifi_signal
    name: "WiFi 信号强度"
    update_interval: 60s  
  # 无线信号强度百分比
  - platform: wifi_signal
    name: "Wireless Signal"
    unit_of_measurement: "%"
    update_interval: 60s
    filters:
      - lambda: return min(max(2 * (x + 100.0), 0.0), 100.0);  //将 -100 ~ 0 dBm 转换为 0 ~ 100% 范围               

